{
  "name": "rcs-code-atlas",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-atlas.mjs",
    "check": "node scripts/generate-atlas.mjs --check",
    "dev": "node scripts/dev.mjs",
    "build": "npm run generate && vite build",
    "test": "node --test \"tests/*.test.mjs\"",
    "ci": "npm run test && npm run build"
  },
  "dependencies": {
    "cytoscape": "^3.33.1",
    "cytoscape-dagre": "^2.5.0",
    "ts-morph": "^27.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "chokidar": "^4.0.3",
    "vite": "^7.1.4",
    "vite-plugin-singlefile": "^2.3.0"
  }
}
