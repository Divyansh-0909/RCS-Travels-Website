import { View } from "react-native";
import AppText from "../components/AppText";

const Rides = () => {
    return (
        <View>
            <AppText>Rides</AppText>
        </View>
    )
}

export default Rides
